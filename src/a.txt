;;; 闭合符号是造成代码嵌套、公式嵌套罪魁祸首
;;; Qwety Bad design for Symbol type
;;; 让代码尽量简洁、可读、易于调试
;;; 让原本的一行并列结构，尽量转成多行并列结构，不害怕多的换行



;; import { } from "vue"
;; 这样的引用格式写法是有问题的，先在行尾指明引入名，后以跑到行中指定引入项




;; 两个分号为一个行注释

;; 自动方法归属注解为编辑器行为，分号是为了归属补全
;Object.keys


;; 字面量字符串
;; s-qwe
;; -qwe-
'qwe'

;; 函数定义
fn foo a a
  a + a

;; 函数调用
2 2
.foo 

;; 函数装饰
time
fn foo a a
  a + a

;; 函数捕获参数 将一次函数链式必须换行
'qwe'
  .foo '2'
